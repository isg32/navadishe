# Copy this file to .env.local for local `vercel dev` testing.
# In production, set this in Vercel → Project Settings → Environment Variables instead.
APPS_SCRIPT_URL=PASTE_YOUR_DEPLOYED_WEB_APP_URL_HERE
